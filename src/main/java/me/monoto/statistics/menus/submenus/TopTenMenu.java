package me.monoto.statistics.menus.submenus;

import dev.triumphteam.gui.builder.item.ItemBuilder;
import dev.triumphteam.gui.guis.Gui;
import me.monoto.statistics.menus.PlayerMenu;
import me.monoto.statistics.menus.utils.BackButton;
import me.monoto.statistics.utils.Formatters;
import net.kyori.adventure.text.Component;
import org.bukkit.entity.Player;

public class TopTenMenu {
    /*
    private static final int size = 3;

    public static void getTopTenPreview(String type, Player player) {

        Gui gui = Gui.gui().rows(3).title(Component.text("Top 10 " + Formatters.capitaliseEachWord(type))).create();

        gui.setItem(4, size, BackButton.getBackButton());

        switch (type) {
            case "fished":
                getFish(gui);
                break;
            case "killed":
                getKills(gui, target);
                break;
            case "moved":
                getMovements(gui, target);
                break;
            case "mined":
            case "placed":
                getBlocks(gui, type, target);
                break;
        }

        gui.setDefaultClickAction(event -> event.setCancelled(true));

        gui.open(player);
    }
     */
}
